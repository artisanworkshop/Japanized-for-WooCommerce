# R2 レビュー結果（検証モード）
- ブランチ: `fix/paidy-receiver-signature-fallback`
- 対象: R1 修正差分のみ（`class-wc-paidy-apply-receiver.php` `check_permissions()` の警告ログ追加、`test-paidy-receiver-signature.php` の end-to-end テスト追加）。HEAD は 27fedf3 のまま（未コミット）
- 判定: **APPROVE**（新規 Critical/High なし）→ **ループ終了**

## R1 指摘の解消判定
| ID | 重大度 | 判定 |
|---|---|---|
| R1-1 | Medium | 解消 — 署名ヘッダーあり・検証失敗時に `paidy-wc` へ warning（drift 秒数・tolerance 付き）。`$timestamp` が null/非数値でも `is_numeric` ガードで `n/a` になり fatal しない |
| R1-2 | Medium | 解消 — AES 暗号化 approved コールバック → `check_permissions` → `handle_receive_data` → キー保存・`currentStep=3`・レスポンスにシークレット非含有 → 同一リクエスト再送で 403 を検証。テスト成功を確認 |

## 指摘（R1 修正で新たに混入したもの）
なし

## Low（backlogへ）
- [R2-L1][Low][includes/gateways/paidy/class-wc-paidy-apply-receiver.php:370] 署名ヘッダー付きの不正リクエストごとに warning を 1 行書くため、意図的な連投でログが膨らみ得る（`paidy_not_configured` / `application_id` 形式チェックが先に走るため無条件ではない。既存の state 失敗経路も同様にログ無しで 403 を返しており、実害は小さい）
